import React from 'react';

const IndexPage = () => {
    return (
        <div>
            <h1>ToDo List</h1>
            {/* ここにToDoリストの表示と追加フォームを実装 */}
        </div>
    );
};

export default IndexPage;
